apiVersion: v1
kind: Service

metadata:
  name: taskgraph-service
  namespace: taskgraph

spec:
  selector:
    app: taskgraph

  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP

  type: NodePort