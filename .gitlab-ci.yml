stages:
  - test
  - docker

test:
  stage: test
  image: python:3.12

  before_script:
    - pip install -r requirements.txt

  script:
    - pytest tests -v

docker-build:
  stage: docker

  image: docker:latest

  services:
    - docker:dind

  script:
    - docker build -t taskgraph .